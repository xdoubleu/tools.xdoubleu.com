name: Main Workflow

permissions:
  actions: write
  contents: write
  packages: write

on:
  workflow_dispatch:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  changes:
    runs-on: ubuntu-latest
    outputs:
      api: ${{ steps.filter.outputs.api }}
      web: ${{ steps.filter.outputs.web }}
      proto: ${{ steps.filter.outputs.proto }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            api:
              - 'api/**'
            web:
              - 'web/**'
            proto:
              - 'proto/**'

  proto-gen:
    needs: changes
    if: |
      github.event_name == 'workflow_dispatch' ||
      needs.changes.outputs.proto == 'true' ||
      needs.changes.outputs.api == 'true' ||
      needs.changes.outputs.web == 'true'
    name: Proto Regeneration Workflow
    uses: ./.github/workflows/proto-gen.yml
    secrets: inherit

  build-api:
    needs: [changes, proto-gen]
    if: |
      always() &&
      (github.event_name == 'workflow_dispatch' ||
      needs.changes.outputs.api == 'true' ||
      needs.changes.outputs.proto == 'true') &&
      (needs.proto-gen.result == 'success' || needs.proto-gen.result == 'skipped')
    name: Build API Workflow
    uses: ./.github/workflows/build-api.yml
    secrets: inherit

  build-web:
    needs: [changes, proto-gen]
    if: |
      always() &&
      (github.event_name == 'workflow_dispatch' ||
      needs.changes.outputs.web == 'true' ||
      needs.changes.outputs.proto == 'true') &&
      (needs.proto-gen.result == 'success' || needs.proto-gen.result == 'skipped')
    name: Build Web Workflow
    uses: ./.github/workflows/build-web.yml
    secrets: inherit

  api-lint:
    needs: [changes, build-api]
    if: |
      always() &&
      (github.event_name == 'workflow_dispatch' ||
      needs.changes.outputs.api == 'true' ||
      needs.changes.outputs.proto == 'true') &&
      needs.build-api.result == 'success'
    name: API Lint Workflow
    uses: ./.github/workflows/api-lint.yml
    secrets: inherit

  web-lint:
    needs: [changes, build-web]
    if: |
      always() &&
      (github.event_name == 'workflow_dispatch' ||
      needs.changes.outputs.web == 'true' ||
      needs.changes.outputs.proto == 'true') &&
      needs.build-web.result == 'success'
    name: Web Lint Workflow
    uses: ./.github/workflows/web-lint.yml
    secrets: inherit

  api-test:
    needs: [changes, build-api]
    if: |
      always() &&
      (github.event_name == 'workflow_dispatch' ||
      needs.changes.outputs.api == 'true' ||
      needs.changes.outputs.proto == 'true') &&
      needs.build-api.result == 'success'
    name: API Test Workflow
    uses: ./.github/workflows/api-test.yml
    secrets: inherit

  web-test:
    needs: [changes, build-web]
    if: |
      always() &&
      (github.event_name == 'workflow_dispatch' ||
      needs.changes.outputs.web == 'true' ||
      needs.changes.outputs.proto == 'true') &&
      needs.build-web.result == 'success'
    name: Web Test Workflow
    uses: ./.github/workflows/web-test.yml
    secrets: inherit

  commit-gen:
    needs: [changes, proto-gen, api-lint, api-test, web-lint, web-test]
    if: |
      always() &&
      needs.proto-gen.result == 'success' &&
      (needs.api-lint.result == 'success' || needs.api-lint.result == 'skipped') &&
      (needs.api-test.result == 'success' || needs.api-test.result == 'skipped') &&
      (needs.web-lint.result == 'success' || needs.web-lint.result == 'skipped') &&
      (needs.web-test.result == 'success' || needs.web-test.result == 'skipped')
    name: Commit Generated Proto Stubs Workflow
    uses: ./.github/workflows/commit-gen.yml
    secrets: inherit

  docker:
    needs: [changes, proto-gen, build-api, build-web, api-lint, web-lint, api-test, web-test, commit-gen]
    if: |
      always() &&
      github.event_name == 'push' &&
      github.ref == 'refs/heads/main' &&
      (needs.proto-gen.result == 'success' || needs.proto-gen.result == 'skipped') &&
      (needs.build-api.result == 'success' || needs.build-api.result == 'skipped') &&
      (needs.build-web.result == 'success' || needs.build-web.result == 'skipped') &&
      (needs.commit-gen.result == 'success' || needs.commit-gen.result == 'skipped')
    name: Docker Build & Push Workflow
    uses: ./.github/workflows/docker.yml
    with:
      build_api: ${{ github.event_name == 'workflow_dispatch' || needs.changes.outputs.api == 'true' || needs.changes.outputs.proto == 'true' }}
      build_web: ${{ github.event_name == 'workflow_dispatch' || needs.changes.outputs.web == 'true' || needs.changes.outputs.proto == 'true' }}
    secrets: inherit
