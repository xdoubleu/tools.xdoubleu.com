{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run publish",
      "type": "go",
      "request": "launch",
      "mode": "auto",
      "program": "${workspaceFolder}/cmd/publish",
      "cwd": "${workspaceFolder}"
    }
  ]
}