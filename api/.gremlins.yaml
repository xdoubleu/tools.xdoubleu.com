# gremlins mutation-testing config, shared by `make test/mutation` (full
# baseline) and `make test/mutation/diff`. Mirrors coverage.out's exclusions
# (generated stubs and mocks).
unleash:
  exclude-files:
    - "(^|/)gen/"
    - "_mock\\.go$"
