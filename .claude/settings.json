{
  "model": "sonnet",
  "autoMemoryEnabled": false,
  "extraKnownMarketplaces": {
    "xdoubleu-claude-plugins": {
      "source": {
        "source": "github",
        "repo": "xdoubleu/xdoubleu-claude-plugins"
      }
    },
    "claude-plugins-official": {
      "source": {
        "source": "github",
        "repo": "anthropics/claude-plugins-official"
      }
    }
  },
  "enabledPlugins": {
    "git-task-flow@xdoubleu-claude-plugins": true,
    "session-retro@xdoubleu-claude-plugins": true,
    "git-cleanup@xdoubleu-claude-plugins": true,
    "github-issue-triage@xdoubleu-claude-plugins": true,
    "skill-lifecycle@xdoubleu-claude-plugins": true,
    "claude-md-management@claude-plugins-official": true
  },
  "permissions": {
    "defaultMode": "plan",
    "allow": [
      "Bash(go test *)",
      "Bash(npm run test:cov *)",
      "Bash(ast-grep run *)",
      "Bash(make test/cov/report)",
      "Bash(go build *)",
      "Bash(go vet *)",
      "Bash(npm run lint *)",
      "Bash(go tool cover *)",
      "Bash(make lint)",
      "Bash(make lint *)",
      "Bash(npm run test:single *)",
      "Bash(make test)",
      "Bash(make test *)",
      "Bash(gh project list *)",
      "Bash(gh project view *)",
      "Bash(gh project item-list *)",
      "Bash(gh project field-list *)",
      "Bash(/usr/bin/log show *)",
      "Bash(docker-compose up -d*)",
      "Bash(docker-compose down*)",
      "Bash(docker-compose ps*)",
      "Bash(make lint/pkg *)",
      "Bash(make pdf/check)",
      "Bash(npm run generate)",
      "Bash(git fetch origin main)",
      "Bash(git fetch origin main *)",
      "Bash(curl -s \"https://api.codecov.io/*\")",
      "mcp__tools-apps__get_deploy_logs",
      "mcp__tools-apps__get_deploy_status",
      "mcp__tools-apps__reading_list_feeds"
    ]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "cmd=$(jq -r '.tool_input.command // \"\"'); if echo \"$cmd\" | grep -Eq '(^|[^a-zA-Z])(grep|rg)([^a-zA-Z]|$)' && ! echo \"$cmd\" | grep -q 'ast-grep'; then printf '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"additionalContext\":\"REMINDER: CLAUDE.md rule - prefer ast-grep over grep/rg for code searches. Only use grep/rg for non-code files (logs, configs, docs).\"}}'; fi",
            "statusMessage": "Checking for grep/rg usage..."
          }
        ]
      },
      {
        "matcher": "Grep",
        "hooks": [
          {
            "type": "command",
            "command": "printf '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"additionalContext\":\"REMINDER: CLAUDE.md rule - prefer ast-grep for code searches. Only use the Grep tool for non-code files (logs, configs, docs).\"}}'",
            "statusMessage": "Reminding to prefer ast-grep..."
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "ExitPlanMode",
        "hooks": [
          {
            "type": "command",
            "command": "printf '{\"hookSpecificOutput\":{\"hookEventName\":\"PostToolUse\",\"additionalContext\":\"REMINDER: approving a plan does NOT start the task. Run the start-task skill now - a fresh worktree off latest main, plus a tracking issue with this approved plan recorded in its ## Plan section - BEFORE the first edit. When the work is done, run finish-task (lint, coverage, build, PR, CI). Opening the PR is standing pre-authorized workflow here and does not need to be requested.\"}}'",
            "statusMessage": "Reminding to run start-task..."
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "input=$(cat); stop_active=$(printf '%s' \"$input\" | jq -r '.stop_hook_active // false'); if [ \"$stop_active\" = \"true\" ]; then exit 0; fi; cwd=$(printf '%s' \"$input\" | jq -r '.cwd // empty'); if [ -z \"$cwd\" ]; then exit 0; fi; case \"$cwd\" in */.claude/worktrees/*) ;; *) exit 0 ;; esac; branch=$(git -C \"$cwd\" rev-parse --abbrev-ref HEAD 2>/dev/null) || exit 0; if [ \"$branch\" = \"main\" ] || [ \"$branch\" = \"HEAD\" ]; then exit 0; fi; ahead=$(git -C \"$cwd\" rev-list --count origin/main..HEAD 2>/dev/null || echo 0); dirty=$(git -C \"$cwd\" status --porcelain 2>/dev/null | head -1); if [ \"$ahead\" = \"0\" ] && [ -z \"$dirty\" ]; then exit 0; fi; if [ -n \"$dirty\" ]; then state=dirty; else state=clean; fi; prs=$(cd \"$cwd\" && gh pr list --head \"$branch\" --state all --json number --jq length 2>/dev/null || echo 0); if [ \"$prs\" != \"0\" ]; then exit 0; fi; common=$(git -C \"$cwd\" rev-parse --path-format=absolute --git-common-dir 2>/dev/null) || exit 0; head_sha=$(git -C \"$cwd\" rev-parse HEAD 2>/dev/null) || exit 0; sd=\"$common/claude-finish-task\"; mkdir -p \"$sd\"; sf=\"$sd/$(printf '%s' \"$branch\" | tr '/' '-')\"; last=\"\"; [ -f \"$sf\" ] && last=$(cat \"$sf\"); key=\"$head_sha-$state\"; if [ \"$last\" = \"$key\" ]; then exit 0; fi; printf '%s' \"$key\" > \"$sf\"; jq -n --arg b \"$branch\" '{\"decision\":\"block\",\"reason\":(\"Branch \\($b) has finished work that was never shipped: commits ahead of origin/main (or uncommitted changes), and no pull request for it. Run the finish-task skill now - lint, coverage, the web build, then ship-pr to open the PR and watch CI to green, then session-retro. Opening the PR is standing pre-authorized workflow for this repo: do NOT wait to be asked, and do NOT stop at having pushed the branch. If the work genuinely is not finished, say what is left and stop - this hook will not fire again for the same commit.\")}'",
            "statusMessage": "Checking for unshipped work..."
          }
        ]
      }
    ]
  }
}
