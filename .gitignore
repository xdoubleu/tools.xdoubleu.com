.env

.DS_Store
__debug_*

# Scratch git worktrees created by parallel Agent-tool subagents
# (isolation: "worktree") - each is its own checkout with its own
# branch, never meant to be committed into this one.
.claude/worktrees/
